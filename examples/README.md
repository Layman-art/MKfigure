# 示例：主动配电网动态电压控制

[下载可编辑 PPTX](./active-grid-office-math.pptx) · [查看 PowerPoint 渲染](../docs/assets/editable-preview.png) · [查看参考图](../docs/assets/reference.png)

这是根据作者提供的参考图，在本项目开发过程中制作的 **1 页、3:2** 复刻示例。

## 编辑结构

- 标题、中文说明、主要框架、箭头和连线是原生 PowerPoint 对象。
- 7 个独立公式区域为 Office 2024 原生 Office Math（OMML / MathZone）：三行系统方程、切换约束、调度目标、LMI 矩阵和控制律。
- LMI 从多个拼接文本框合并成一个原生矩阵公式。
- 中文标签中的短变量仍保留真实上下标文本，避免混排转换改变中文字体与行高。
- 复杂设备、拓扑插画与图标为嵌入 PNG，不保证内部元素逐个可编辑。

## 已验证与未验证

已在 Windows x64 + PowerPoint 2024 中保存重开并检查 7 个 MathZone；PPTX 含 7 个 `a14:m` 和 7 个 `m:oMath`，含矩阵与求和结构，无 MathType OLE、宏或外部图片依赖。原生渲染和页面溢出检查已完成。

本示例是版面复刻与编辑能力展示，不是仿真模型、实验数据集或论文复现。没有独立验证图中物理结论和数字。原图中的“3 个 DG”与设备数、重复 DG2、缺少求和/最小化下标，以及故障标题与正文等疑点未擅自修正。

参考图包含 AI 辅助生成的视觉内容。作者授权将本工作流示例随仓库发布；这不保证图中所有事实或视觉元素适用于商业、商标或学术引用用途。第三方软件、字体和标识不因本仓库许可证获得额外授权。

公开副本仅规范化文档作者元数据并移除最后修改者标识；幻灯片、公式和嵌入素材未改动。README 预览来自同一页面的真实 PowerPoint 渲染。

只需私发本目录的 `active-grid-office-math.pptx`，无需附带脚本或图标素材。编辑原生公式请使用支持 Office Math 的 PowerPoint；非 Office 预览器的兼容性未作保证。
