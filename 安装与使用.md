# MK Figure 0.1.0

## 下载和安装

| 电脑 | 安装文件 |
|---|---|
| Windows 64位（Intel/AMD） | [Windows 安装程序](release/MK-Figure-0.1.0-Windows-x64-Setup.exe) |
| Mac，Apple Silicon（M系列芯片） | [Apple Silicon 应用包](release-mac/MK-Figure-0.1.0-macOS-arm64.zip) |
| Mac，Intel 芯片 | [Intel 应用包](release-mac/MK-Figure-0.1.0-macOS-x64.zip) |

Windows 运行 EXE 并按安装向导选择位置。Mac 需要 macOS 13 或更新版本；解压 ZIP 后将 `MK Figure.app` 拖入“应用程序”。Mac 包是实际应用 bundle ZIP，不是 DMG。

已内置程序运行环境和 Codex CLI，不需要另外安装 Node.js、Python 或 PowerPoint。默认科研字体不随软件分发；若设备缺少微软雅黑或 Times New Roman，预览可能采用替代字体，程序会提示。

## 第一次打开

1. 点击右上角“模型与连接”。
2. **使用 Codex 额度**：选择 Codex，点击“检测连接与模型”。已有官方 Codex 登录可直接复用；未登录时点击“登录 Codex 账户”，在浏览器完成登录后再检测。
3. 从返回的模型列表中选择模型，保存设置。模型是否可用取决于账户实际权限和额度。
4. 如果使用 OpenAI、DeepSeek 或其他兼容 API，填写服务地址、API Key 和对应模型 ID。图片复刻需要模型支持图片输入。生图服务在设置下半部分单独选择。

Codex 生图通过选定 Codex 模型调用图像工具，不能据此指定或确认其内部 Image 2.5 变体。使用 OpenAI 兼容图片 API 时，可填写该服务支持的明确图像模型 ID。

## 开始画图

- **从想法开始**：输入主题与重点；论文、方法资料、语言用途、参考图都可以按需补充。可以整理内容、生成并编辑提示词，再生成视觉稿、复刻、检查和导出。
- **已经有图片**：直接上传要复刻的图片，跳到“可编辑复刻”。
- **打开离线示例**：不连接模型也能体验预览、文字编辑和 PPTX/SVG/PNG 导出。

内置三种参考风格，也能上传自己的参考图。步骤可以自由切换，不必机械地从第一步开始。

复刻后先检查文字、公式和连线。发现问题可填写“精修意见”，或直接修改图中文字。PPT 的文字、形状、曲线和公式组件均为原生对象；公式使用衬线文字组件，不使用 Office Math。复杂公式改字后可能需要调整组内排版。

## 本版的实际验证范围

Windows 打包版启动、资料导入、导出和编辑性已实际验证；Codex 生图、复刻、检查、精修均有真实调用记录。部分样例仍存在公式间距和还原简化提示，自动检查不代表任意图都一次合格。

Mac 两架构包完成了结构、架构、权限、资源和本地 ad-hoc 签名的离线验证；**尚未在真实 Mac 上运行，未做 Developer ID 签名或 Apple 公证**。Windows 安装包也未使用发行者数字证书。正式公开分发前需完成相应系统的实机测试和签名。

OpenAI / DeepSeek / 自定义 API 的协议测试通过，但本次没有相应付费 Key，未做真实付费 API 调用。PDF/PPTX 目前是文字提取，扫描版或重要图表请补充页面图片。

完整记录：[验收说明](docs/validation.md)、[Mac 包验证范围](docs/mac-package-validation.md)。
